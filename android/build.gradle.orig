// Backup of original android/build.gradle
// If you need to restore Android APK build functionality, rename this file back to build.gradle
// Original content saved here.

// Minimal Android module build.gradle
// Use legacy buildscript classpath to supply the Android Gradle plugin so Gradle can resolve it
buildscript {
    repositories {
        google()
        mavenCentral()
        mavenLocal()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
    }
}

apply plugin: 'com.android.application'

android {
    namespace "io.github.Fishing_joy"
    compileSdkVersion 35

    defaultConfig {
        applicationId "io.github.Fishing_joy"
        minSdkVersion 21
        targetSdkVersion 35
        versionCode 1
        versionName "1.0"
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    packagingOptions {
        // avoid packaging duplicate files if any
        exclude "META-INF/*.kotlin_module"
    }

    // Include the project root assets folder so existing assets are packaged
    sourceSets {
        main {
            assets.srcDirs += ["$rootDir/assets"]
        }
    }
}

repositories {
    google()
    mavenCentral()
    mavenLocal()
}

dependencies {
    implementation project(':core')
    implementation "com.badlogicgames.gdx:gdx-backend-android:$gdxVersion"
    implementation "com.badlogicgames.gdx:gdx-box2d:$gdxVersion"
}

